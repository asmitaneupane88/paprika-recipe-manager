<local:NavigatorPage
    x:Class="RecipeApp.Controls.Pages.StepViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeApp.Controls.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:RecipeApp.Models"
    xmlns:recipeSteps="clr-namespace:RecipeApp.Models.RecipeSteps"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Button Grid.Column="0" Click="ButtonAddRecipe_OnClick" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="8" IsHitTestVisible="False" Margin="10">
                <TextBlock Text="Add Recipe To Cook" VerticalAlignment="Center" IsHitTestVisible="False"/>
                <SymbolIcon Symbol="Add" IsHitTestVisible="False"/>
            </StackPanel>
        </Button>

        <Button Grid.Row="0" Grid.Column="2" Click="ButtonResetAll_OnClick" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal" Spacing="8" IsHitTestVisible="False" Margin="10">
                <TextBlock Text="Clear All Steps" VerticalAlignment="Center" IsHitTestVisible="False"/>
                <SymbolIcon Symbol="Refresh" IsHitTestVisible="False"/>
            </StackPanel>
        </Button>

        <ScrollView Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ItemsControl ItemsSource="{x:Bind Steps, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:ActiveStepInfo">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                Margin="8">
                            <StackPanel Spacing="12" Tag="{x:Bind}">
                                <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                                    <Border Margin="5"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Top"
                                            CornerRadius="10"
                                            BorderThickness="2"
                                            BorderBrush="{ThemeResource PrimaryColor}"
                                            Background="{ThemeResource SurfaceVariantColor}">
                                        <Image
                                            Source="{x:Bind RecipeImageUrl}"
                                            Width="40" Height="40"
                                            Stretch="UniformToFill"/>
                                    </Border>
                                    <TextBlock Text="{x:Bind RecipeTitle}"
                                               FontSize="18"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Text="{x:Bind CurrentStep.BindableTitle, Mode=OneWay}"
                                           FontSize="16"
                                           FontWeight="Medium"
                                           HorizontalAlignment="Center"/>

                                <ContentPresenter Content="{x:Bind BindableContent, Mode=OneWay}" HorizontalAlignment="Center"/>

                                <ItemsControl ItemsSource="{x:Bind CurrentStep.BindableGetOutNodes, Mode=OneWay}" Visibility="{x:Bind NextButtonsVisible, Mode=OneWay}" HorizontalAlignment="Center">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="recipeSteps:OutNode">
                                            <Button Content="{x:Bind Title}" Tag="{x:Bind}" Click="ButtonOutNode_OnClick"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollView>
    </Grid>
</local:NavigatorPage>

