<local:NavigatorPage
    x:Class="RecipeApp.Controls.Pages.RecipePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeApp.Controls.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:RecipeApp.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox
            Grid.Row="0"
            Grid.Column="0"
            PlaceholderText="Search Recipes..."
            Margin="10"
            Text="{x:Bind SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            Width="300"/>

        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="10" IsEnabled="{x:Bind CardsSelected, Mode=OneWay}" Click="OnButtonPrintClick">
                <SymbolIcon Symbol="Print"/>
            </Button>
            <Button Margin="10" IsEnabled="{x:Bind CardsSelected, Mode=OneWay}" Click="OnButtonGroceryListClick">
                <SymbolIcon Symbol="Shop"/>
            </Button>
            <Button Margin="10" IsEnabled="{x:Bind CardsSelected, Mode=OneWay}" Click="OnButtonRemoveClick">
                <SymbolIcon Symbol="Remove"/>
            </Button>
            <Button Margin="10" Click="OnButtonAddClick">
                <SymbolIcon Symbol="Add"/>
            </Button>
        </StackPanel>

        <ListView
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            ItemsSource="{x:Bind FilteredRecipes, Mode=OneWay}"
            IsItemClickEnabled="True"
            ItemClick="OnRecipeCardClicked">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:RecipeCard">
                    <Border BorderBrush="{ThemeResource OutlineColor}" BorderThickness="1" Margin="10" Padding="10" CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Margin="10"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    CornerRadius="10"
                                    BorderThickness="2"
                                    BorderBrush="{ThemeResource PrimaryColor}">
                                <Image
                                    Source="{x:Bind Recipe.ImageUrl}"
                                    Width="80" Height="80"
                                    Stretch="UniformToFill"/>
                            </Border>


                            <StackPanel Grid.Column="1" VerticalAlignment="Center"
                                        Margin="10" >
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{x:Bind Recipe.Title}"
                                               FontWeight="Bold"
                                               FontSize="18" />
                                    <RatingControl Grid.Column="1"
                                                   Value="{x:Bind Recipe.Rating}"
                                                   MaxRating="{x:Bind Recipe.BindableMaxRating}"
                                                   IsReadOnly="True"/>
                                </Grid>
                                <TextBlock Text="{x:Bind Recipe.Description}"
                                           MaxLines="3"/>
                            </StackPanel>

                            <!--Got to use "Binding" here over x:Bind-->
                            <CheckBox
                                Grid.Column="2"
                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                Checked="OnRecipeCardChecked"
                                Unchecked="OnRecipeCardChecked"
                                Margin="10"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:NavigatorPage>
